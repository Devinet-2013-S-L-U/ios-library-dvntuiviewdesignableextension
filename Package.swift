// swift-tools-version:5.1

import PackageDescription

let package = Package(
    name: "DVNTUIViewDesignableExtension",
    products: [
        .library(
            name: "DVNTUIViewDesignableExtension",
            targets: ["DVNTUIViewDesignableExtension"]),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "DVNTUIViewDesignableExtension",
            dependencies: [])
    ]
)
